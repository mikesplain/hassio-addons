server {
    listen 8099 default_server;

    include /etc/nginx/includes/server_params.conf;

    allow   172.30.32.2;
    allow    all;

    # location ~ .php$ {
    #     fastcgi_pass 127.0.0.1:9001;
    #     fastcgi_read_timeout 900;
    #     fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #     fastcgi_index index.php;
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     include /etc/nginx/includes/fastcgi.conf;
    # }
    location / {
        # proxy_set_header Host $host;
        # proxy_set_header X-Real-IP $remote_addr;
        # proxy_pass https://assistant.lizandmikes.house:443;
        proxy_pass https://10.0.55.8:443;

        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;

        proxy_ssl_trusted_certificate /etc/nginx/ca.crt;
        proxy_set_header Authorization "Bearer REDACTED";
    }

}
